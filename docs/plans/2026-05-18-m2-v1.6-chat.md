# M2 v1.6-chat — AI 동반자가 실제로 답하게 (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1.5에서 만든 부품(system-prompt·persona-router·가드·safety 컴포넌트)을 AI SDK v6 + Vercel AI Gateway에 연결해, K1 가드를 지키며 streaming으로 답하는 AI 동반자 chat을 동작시킨다.

**Architecture:** Next.js 16 App Router route handler가 입력 가드(응급/정신건강 → AI 스킵 후 escalation) → persona/area 분류 → `buildSystemPrompt()` → `streamText()` (Gateway, plain model string) → 스트림 응답. 메시지는 `ai_chats`/`ai_messages`(v1.5 마이그레이션·RLS)에 저장. UI는 `@ai-sdk/react` `useChat` + 가드 escalation 분기.

**Tech Stack:** AI SDK v6 (`ai`, `@ai-sdk/react`) · Vercel AI Gateway (`anthropic/claude-sonnet-4.6`, `AI_GATEWAY_API_KEY`) · Supabase (ai_chats/ai_messages RLS) · Vitest · Playwright.

**Spec reference:** `docs/plans/2026-05-14-m2-ai-companion-skeleton.md` (Task 3·6·9·11·12 일부) · `docs/specs/2026-05-14-open-questions-resolutions.md` §1.

**비범위 (별도 plan):** RAG ingest/retrieve = `v1.6-rag` · 페어 일기 = `v1.6-diary`.

**전제 (user 발급 — 없으면 Task 5·7 verify만 보류, 나머지 진행 가능):**
- `.env.local`에 `AI_GATEWAY_API_KEY=...` (Vercel AI Gateway)
- (RAG 아님 — HF token은 v1.6-rag에서)

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `package.json` | `ai`·`@ai-sdk/react` 의존 추가 | 수정 |
| `.env.example` | `AI_GATEWAY_API_KEY` 문서화 | 수정 |
| `lib/ai/client.ts` | 모델 ID 상수 + Gateway provider 옵션(disallowPromptTraining) | 신규 |
| `lib/ai/area-router.ts` | 입력 → A~E 영역 분류 (keyword rule) | 신규 |
| `lib/ai/personal-context.ts` | `personal_context` read → `PersonalContext` | 신규 |
| `lib/chat/queries.ts` | chat/메시지 read (RLS 경유) | 신규 |
| `lib/chat/mutations.ts` | chat 생성·메시지 저장 | 신규 |
| `app/(app)/chat/api/route.ts` | POST streaming: 가드→분류→prompt→streamText→저장 | 신규 |
| `app/(app)/chat/page.tsx` | chat 목록 | 신규 |
| `app/(app)/chat/new/page.tsx` | 새 chat (visibility 선택) | 신규 |
| `app/(app)/chat/[chatId]/page.tsx` | thread (useChat) | 신규 |
| `components/chat/ChatList.tsx`·`ChatMessage.tsx`·`ChatComposer.tsx`·`AreaBadge.tsx` | chat UI 조각 | 신규 |
| `tests/unit/ai/area-router.test.ts` | area 분류 테스트 | 신규 |
| `tests/unit/ai/personal-context.test.ts` | personal_context read 테스트 | 신규 |
| `tests/unit/chat/chat-rls.test.ts` | chat/메시지 RLS 격리 | 신규 |
| `tests/e2e/ai-companion.spec.ts` | chat 골든 path + 신체응급 분리 | 신규 |

---

## Task 0: AI SDK 설치 + 환경 변수 문서화

**Files:** Modify `package.json`, `.env.example`

- [ ] **Step 1: AI SDK 설치**

Run: `npm install ai @ai-sdk/react`
Expected: `package.json` dependencies에 `ai`, `@ai-sdk/react` 추가, 에러 0.

- [ ] **Step 2: `.env.example`에 Gateway 키 문서화**

`.env.example` 끝에 추가:
```
# Vercel AI Gateway (Claude Sonnet 4.6 호출). vercel.com 대시보드 → 프로젝트 → AI Gateway → API Key
AI_GATEWAY_API_KEY=
```

- [ ] **Step 3: 빌드 확인**

Run: `npm run build`
Expected: 에러 0 (의존만 추가, 코드 변화 없음).

- [ ] **Step 4: 커밋**

```bash
git add package.json package-lock.json .env.example
git commit -m "chore: M2 v1.6 - add AI SDK v6, document AI_GATEWAY_API_KEY"
```

---

## Task 1: AI 클라이언트 설정 (`lib/ai/client.ts`)

**Files:** Create `lib/ai/client.ts`

모델 ID와 Gateway provider 옵션을 한 곳에 둔다. AI SDK v6는 plain string 모델 ID + `AI_GATEWAY_API_KEY` env면 자동으로 Gateway 라우팅하므로 provider 패키지(`@ai-sdk/anthropic`) 불필요. 의료 민감정보 → `disallowPromptTraining: true`.

- [ ] **Step 1: 작성**

```typescript
// lib/ai/client.ts
import type { GatewayProviderOptions } from "@ai-sdk/gateway";

/**
 * Vercel AI Gateway 모델 ID. AI SDK v6는 plain string + AI_GATEWAY_API_KEY
 * 환경변수면 자동으로 Gateway 경유. 모델 교체는 이 상수만 변경(M6 a/b).
 * 정확한 모델 ID 목록: vercel.com/docs/ai-gateway/models-and-providers
 */
export const CHAT_MODEL = "anthropic/claude-sonnet-4.6";

/** 의료 민감 대화 → 프롬프트 학습 비허용 (K4 개인정보 정합). */
export const CHAT_PROVIDER_OPTIONS = {
  gateway: { disallowPromptTraining: true },
} satisfies { gateway: GatewayProviderOptions };
```

- [ ] **Step 2: 타입 확인**

Run: `npx tsc --noEmit`
Expected: 에러 0. (`@ai-sdk/gateway`는 `ai` 설치 시 함께 들어옴 — 없으면 `npm i @ai-sdk/gateway`)

- [ ] **Step 3: 커밋**

```bash
git add lib/ai/client.ts
git commit -m "feat: M2 v1.6 - AI Gateway client constants (Sonnet 4.6, no prompt training)"
```

---

## Task 2: 영역 분류기 (`lib/ai/area-router.ts`) — TDD

**Files:** Create `lib/ai/area-router.ts`, `tests/unit/ai/area-router.test.ts`

skeleton Task 6. M2는 keyword rule (LLM 호출 없음 — 비용·latency 회피). 결과는 `ai_chats.area` 저장 + UI badge용. 우선순위: 응급 영역은 가드가 별도 처리하므로 여기선 A~E + mixed만.

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/unit/ai/area-router.test.ts
import { describe, it, expect } from "vitest";
import { classifyArea } from "@/lib/ai/area-router";

describe("classifyArea", () => {
  it("A: 일상 do/don't", () => {
    expect(classifyArea("커피 마셔도 되나요")).toBe("A");
    expect(classifyArea("운동 해도 괜찮을까요")).toBe("A");
  });
  it("B: 시술·검사 해석", () => {
    expect(classifyArea("AMH 수치가 1.2 나왔는데")).toBe("B");
    expect(classifyArea("초음파 결과 난포가")).toBe("B");
  });
  it("C: 멘탈", () => {
    expect(classifyArea("요즘 너무 우울하고 불안해요")).toBe("C");
  });
  it("D: 부부 갈등", () => {
    expect(classifyArea("남편이랑 또 싸웠어요")).toBe("D");
  });
  it("E: 의사결정", () => {
    expect(classifyArea("이번에 시술 그만둘지 말지 결정 못 하겠어요")).toBe("E");
    expect(classifyArea("비용이 너무 부담돼서 계속해야 할지")).toBe("E");
  });
  it("mixed: 신호 없음/복합", () => {
    expect(classifyArea("안녕하세요")).toBe("mixed");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run tests/unit/ai/area-router.test.ts`
Expected: FAIL — "classifyArea is not a function" / 모듈 없음.

- [ ] **Step 3: 구현**

```typescript
// lib/ai/area-router.ts
export type Area = "A" | "B" | "C" | "D" | "E" | "mixed";

const RULES: { area: Exclude<Area, "mixed">; kw: string[] }[] = [
  { area: "E", kw: ["결정", "그만둘지", "포기할지", "할지 말지", "비용", "돈", "지원금", "안 되면"] },
  { area: "D", kw: ["남편", "아내", "배우자", "싸웠", "싸움", "갈등", "서운", "오해"] },
  { area: "C", kw: ["우울", "불안", "무너", "힘들", "지쳐", "번아웃", "외로", "눈물"] },
  { area: "B", kw: ["AMH", "수치", "초음파", "난포", "정액검사", "혈액검사", "배아", "착상", "결과지", "호르몬"] },
  { area: "A", kw: ["먹어도", "마셔도", "커피", "술", "운동", "음식", "영양제", "습관", "되나요", "괜찮을까"] },
];

/** 입력 첫 메시지 → 영역. 여러 영역 hit 또는 0 hit → "mixed". */
export function classifyArea(input: string): Area {
  const hits = RULES.filter((r) => r.kw.some((k) => input.includes(k)));
  if (hits.length === 1) return hits[0].area;
  return "mixed";
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run tests/unit/ai/area-router.test.ts`
Expected: PASS (6/6). "안 되면"은 E, "되나요"는 A — 단일 hit. 복합/무신호는 mixed.

- [ ] **Step 5: 커밋**

```bash
git add lib/ai/area-router.ts tests/unit/ai/area-router.test.ts
git commit -m "feat: M2 v1.6 - area router (A-E keyword classifier) [TDD]"
```

---

## Task 3: Personal context read (`lib/ai/personal-context.ts`) — TDD

**Files:** Create `lib/ai/personal-context.ts`, `tests/unit/ai/personal-context.test.ts`

skeleton Task 11. `personal_context` 테이블(v1.5 마이그레이션, couple당 1행) read → `system-prompt.ts`의 `PersonalContext` 형으로 변환. M2는 read만 (write는 M3 트래커). 모든 필드 null이면 빈 객체. 테스트는 `couple-isolation.test.ts` 패턴(admin service client, makeUser, 직접 insert).

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/unit/ai/personal-context.test.ts
import { describe, it, expect } from "vitest";
import { createClient } from "@supabase/supabase-js";
import { mapPersonalContext } from "@/lib/ai/personal-context";

const url = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!;
const admin = createClient(url, serviceKey, { auth: { persistSession: false } });

let seq = 0;
async function makeCouple() {
  const email = `pc-${Date.now()}-${seq++}@example.com`;
  const { data: u } = await admin.auth.admin.createUser({
    email, password: "TestPass123!", email_confirm: true,
  });
  const { data: c } = await admin
    .from("couples").insert({ created_by: u!.user!.id }).select().single();
  await admin.from("couple_members").insert({ couple_id: c!.id, user_id: u!.user!.id });
  return c!.id as string;
}

describe("mapPersonalContext", () => {
  it("returns empty object when no row exists", async () => {
    const coupleId = await makeCouple();
    expect(await mapPersonalContext(admin, coupleId)).toEqual({});
  });

  it("maps stored fields to PersonalContext shape", async () => {
    const coupleId = await makeCouple();
    await admin.from("personal_context").insert({
      couple_id: coupleId,
      cycle_stage: "stim",
      cycle_number: 2,
      recent_emotional_state: "지쳐 있음",
    });
    const pc = await mapPersonalContext(admin, coupleId);
    expect(pc.cycleStage).toBe("stim");
    expect(pc.cycleNumber).toBe(2);
    expect(pc.recentEmotionalState).toBe("지쳐 있음");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx dotenv -e .env.local -- vitest run tests/unit/ai/personal-context.test.ts`
Expected: FAIL — `mapPersonalContext` 없음.

- [ ] **Step 3: 구현**

```typescript
// lib/ai/personal-context.ts
import type { SupabaseClient } from "@supabase/supabase-js";
import type { PersonalContext } from "@/lib/ai/system-prompt";

/**
 * couple의 personal_context 행 → PersonalContext. 행 없거나 모든 필드
 * null이면 {}. `db`는 RLS 적용 server client 또는 service client 모두 가능.
 */
export async function mapPersonalContext(
  db: SupabaseClient,
  coupleId: string,
): Promise<PersonalContext> {
  const { data } = await db
    .from("personal_context")
    .select(
      "cycle_stage, cycle_number, current_medications, recent_emotional_state, recent_couple_issues",
    )
    .eq("couple_id", coupleId)
    .maybeSingle();
  if (!data) return {};
  const pc: PersonalContext = {};
  if (data.cycle_stage) pc.cycleStage = data.cycle_stage;
  if (data.cycle_number != null) pc.cycleNumber = data.cycle_number;
  if (data.current_medications)
    pc.currentMedications = data.current_medications as Record<string, unknown>;
  if (data.recent_emotional_state)
    pc.recentEmotionalState = data.recent_emotional_state;
  if (data.recent_couple_issues)
    pc.recentCoupleIssues = data.recent_couple_issues;
  return pc;
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx dotenv -e .env.local -- vitest run tests/unit/ai/personal-context.test.ts`
Expected: PASS (2/2).

- [ ] **Step 5: 커밋**

```bash
git add lib/ai/personal-context.ts tests/unit/ai/personal-context.test.ts
git commit -m "feat: M2 v1.6 - personal_context read mapper [TDD]"
```

---

## Task 4: Chat queries / mutations + RLS 테스트 — TDD

**Files:** Create `lib/chat/queries.ts`, `lib/chat/mutations.ts`, `tests/unit/chat/chat-rls.test.ts`

`ai_chats`/`ai_messages`(v1.5 마이그레이션·RLS) read/write. RLS: pair → 같은 couple 둘 다, private → owner만. 테스트는 `couple-isolation.test.ts` 패턴 + `signInAs`로 RLS 검증.

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/unit/chat/chat-rls.test.ts
import { describe, it, expect, beforeAll } from "vitest";
import { createClient } from "@supabase/supabase-js";

const url = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const anonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;
const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!;
const admin = createClient(url, serviceKey, { auth: { persistSession: false } });

async function makeUser(email: string) {
  const { data } = await admin.auth.admin.createUser({
    email, password: "TestPass123!", email_confirm: true,
  });
  return data!.user!.id;
}
async function signInAs(email: string) {
  const c = createClient(url, anonKey, { auth: { persistSession: false } });
  await c.auth.signInWithPassword({ email, password: "TestPass123!" });
  return c;
}

describe("ai_chats RLS", () => {
  let aliceId: string, bobId: string, malloryId: string, coupleId: string;
  const ts = Date.now();
  const aliceEmail = `chat-a-${ts}@example.com`;
  const malloryEmail = `chat-m-${ts}@example.com`;

  beforeAll(async () => {
    aliceId = await makeUser(aliceEmail);
    bobId = await makeUser(`chat-b-${ts}@example.com`);
    malloryId = await makeUser(malloryEmail);
    const { data: c } = await admin
      .from("couples").insert({ created_by: aliceId }).select().single();
    coupleId = c!.id;
    await admin.from("couple_members").insert([
      { couple_id: coupleId, user_id: aliceId },
      { couple_id: coupleId, user_id: bobId },
    ]);
  });

  it("pair chat: partner can read", async () => {
    const { data: chat } = await admin
      .from("ai_chats")
      .insert({ couple_id: coupleId, owner_user_id: aliceId, visibility: "pair" })
      .select().single();
    const bob = await signInAs(`chat-b-${ts}@example.com`);
    const { data } = await bob
      .from("ai_chats").select().eq("id", chat!.id).maybeSingle();
    expect(data?.id).toBe(chat!.id);
  });

  it("private chat: partner cannot read", async () => {
    const { data: chat } = await admin
      .from("ai_chats")
      .insert({ couple_id: coupleId, owner_user_id: aliceId, visibility: "private" })
      .select().single();
    const bob = await signInAs(`chat-b-${ts}@example.com`);
    const { data } = await bob
      .from("ai_chats").select().eq("id", chat!.id).maybeSingle();
    expect(data).toBeNull();
  });

  it("non-member cannot read pair chat", async () => {
    const { data: chat } = await admin
      .from("ai_chats")
      .insert({ couple_id: coupleId, owner_user_id: aliceId, visibility: "pair" })
      .select().single();
    const mallory = await signInAs(malloryEmail);
    const { data } = await mallory
      .from("ai_chats").select().eq("id", chat!.id).maybeSingle();
    expect(data).toBeNull();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx dotenv -e .env.local -- vitest run tests/unit/chat/chat-rls.test.ts`
Expected: 만약 FAIL이면 v1.5 RLS 정책 점검. PASS여야 정상(RLS는 v1.5 마이그레이션에 존재) — 이 테스트는 RLS 회귀 가드. 통과 확인되면 다음 단계로(테스트가 이미 green이면 RLS 동작 검증 완료).

> 주의: 이 Task는 RLS가 v1.5에 이미 있어 테스트가 바로 통과할 수 있다. 그 경우 "기존 동작 회귀 가드"로 둔다(삭제 금지). queries/mutations 구현은 Step 3에서 별도 검증.

- [ ] **Step 3: queries / mutations 구현**

```typescript
// lib/chat/queries.ts
import { createClient } from "@/lib/supabase/server";

export async function listChats() {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from("ai_chats")
    .select("id, title, visibility, area, persona, updated_at")
    .order("updated_at", { ascending: false });
  if (error) throw error;
  return data;
}

export async function getChat(chatId: string) {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from("ai_chats").select("*").eq("id", chatId).maybeSingle();
  if (error) throw error;
  return data;
}

export async function getMessages(chatId: string) {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from("ai_messages")
    .select("id, role, content, guard_triggered, sources, created_at")
    .eq("chat_id", chatId)
    .order("created_at", { ascending: true });
  if (error) throw error;
  return data;
}
```

```typescript
// lib/chat/mutations.ts
import type { SupabaseClient } from "@supabase/supabase-js";

export async function createChat(
  db: SupabaseClient,
  args: {
    coupleId: string;
    ownerUserId: string;
    visibility: "pair" | "private";
    title?: string;
    area?: string | null;
    persona?: string | null;
  },
) {
  const { data, error } = await db
    .from("ai_chats")
    .insert({
      couple_id: args.coupleId,
      owner_user_id: args.ownerUserId,
      visibility: args.visibility,
      title: args.title ?? null,
      area: args.area ?? null,
      persona: args.persona ?? null,
    })
    .select()
    .single();
  if (error) throw error;
  return data;
}

export async function addMessage(
  db: SupabaseClient,
  args: {
    chatId: string;
    role: "user" | "assistant" | "system";
    content: string;
    guardTriggered?: string[];
    sources?: unknown;
  },
) {
  const { error } = await db.from("ai_messages").insert({
    chat_id: args.chatId,
    role: args.role,
    content: args.content,
    guard_triggered: args.guardTriggered ?? null,
    sources: args.sources ?? null,
  });
  if (error) throw error;
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx dotenv -e .env.local -- vitest run tests/unit/chat/chat-rls.test.ts`
Expected: PASS (3/3). 타입: `npx tsc --noEmit` 에러 0.

- [ ] **Step 5: 커밋**

```bash
git add lib/chat/ tests/unit/chat/chat-rls.test.ts
git commit -m "feat: M2 v1.6 - chat queries/mutations + RLS regression test [TDD]"
```

---

## Task 5: Streaming chat route (`app/(app)/chat/api/route.ts`)

**Files:** Create `app/(app)/chat/api/route.ts`

핵심. 흐름: 인증 → 입력 가드(응급/정신건강 hit → AI 호출 0, escalation 신호 + 메시지 저장) → persona/area 분류 → personal_context → `buildSystemPrompt` → 과거 메시지 + 신규 → `streamText`(Gateway) → 스트림 응답 + `onFinish`에서 assistant 메시지·후처리 가드 저장.

가드 함수 시그니처는 v1.5 파일에서 확인 후 사용:
- `lib/ai/guards/emergency-keywords.ts` · `lib/ai/guards/mental-health-emergency.ts` (입력 검사)
- `lib/ai/guards/statistics-citation.ts` · `lib/ai/guards/diagnosis-boundary.ts` (응답 후처리 로깅)
- `lib/ai/persona-router.ts` `classifyPersona(input, meta)`
- `lib/ai/system-prompt.ts` `buildSystemPrompt({persona, visibility, personalContext})`

- [ ] **Step 1: v1.5 가드 export 시그니처 확인**

Run: 읽기 — `lib/ai/guards/emergency-keywords.ts`, `lib/ai/guards/mental-health-emergency.ts`
확인할 것: export 함수명·반환형 (예: `detectEmergency(input): boolean` / `{hit, channels}`). 실제 시그니처에 맞춰 Step 2 코드의 import·호출부를 조정한다. (플랜은 `detectPhysicalEmergency`/`detectMentalHealthEmergency`가 `boolean` 반환이라고 가정 — 다르면 맞춘다.)

- [ ] **Step 2: route 작성**

```typescript
// app/(app)/chat/api/route.ts
import { streamText } from "ai";
import { createClient as createServiceClient } from "@supabase/supabase-js";
import { requireUser } from "@/lib/auth/current-user";
import { getCoupleForUser } from "@/lib/couple/queries";
import { CHAT_MODEL, CHAT_PROVIDER_OPTIONS } from "@/lib/ai/client";
import { buildSystemPrompt } from "@/lib/ai/system-prompt";
import { classifyPersona } from "@/lib/ai/persona-router";
import { classifyArea } from "@/lib/ai/area-router";
import { mapPersonalContext } from "@/lib/ai/personal-context";
import { addMessage } from "@/lib/chat/mutations";
import { detectPhysicalEmergency } from "@/lib/ai/guards/emergency-keywords";
import { detectMentalHealthEmergency } from "@/lib/ai/guards/mental-health-emergency";

const admin = () =>
  createServiceClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false } },
  );

type Body = {
  chatId: string;
  message: string;
  visibility: "pair" | "private";
  meta?: { cycleCount?: number; yearsTrying?: number; maleFactor?: boolean };
};

export async function POST(request: Request) {
  const user = await requireUser();
  const { chatId, message, visibility, meta = {} } = (await request.json()) as Body;
  const db = admin();

  // 1. 입력 가드 — 응급/정신건강이면 AI 호출 0
  const physical = detectPhysicalEmergency(message);
  const mental = detectMentalHealthEmergency(message);
  if (physical || mental) {
    const kind = physical ? "physical_emergency" : "mental_health_emergency";
    await addMessage(db, { chatId, role: "user", content: message });
    await addMessage(db, {
      chatId,
      role: "assistant",
      content: "",
      guardTriggered: [kind],
    });
    return Response.json({ escalation: kind });
  }

  // 2. 분류 + 컨텍스트
  const persona = classifyPersona(message, meta);
  const area = classifyArea(message);
  const coupleId = await getCoupleForUser(user.id);
  const personalContext = coupleId
    ? await mapPersonalContext(db, coupleId)
    : {};
  const system = buildSystemPrompt({ persona, visibility, personalContext });

  // 3. 과거 메시지 + 신규
  const { data: history } = await db
    .from("ai_messages")
    .select("role, content")
    .eq("chat_id", chatId)
    .in("role", ["user", "assistant"])
    .order("created_at", { ascending: true });
  const messages = [
    ...(history ?? []).map((m) => ({
      role: m.role as "user" | "assistant",
      content: m.content,
    })),
    { role: "user" as const, content: message },
  ];

  await addMessage(db, { chatId, role: "user", content: message });

  // 4. 스트리밍 (Gateway, plain model string)
  const result = streamText({
    model: CHAT_MODEL,
    system,
    messages,
    providerOptions: CHAT_PROVIDER_OPTIONS,
    onFinish: async ({ text }) => {
      await addMessage(db, { chatId, role: "assistant", content: text });
      await db
        .from("ai_chats")
        .update({ area, persona, updated_at: new Date().toISOString() })
        .eq("id", chatId);
    },
  });

  return result.toTextStreamResponse();
}
```

- [ ] **Step 3: 가드 시그니처 정합 확인**

Run: `npx tsc --noEmit`
Expected: 에러 0. 만약 `detectPhysicalEmergency`/`detectMentalHealthEmergency` 이름·형이 다르면 v1.5 실제 export에 맞게 import·호출부 수정 후 재실행.

- [ ] **Step 4: (key 있을 때) 수동 스모크**

`.env.local`에 `AI_GATEWAY_API_KEY` 설정 후:
Run: `npm run dev` → 별도 터미널
```bash
curl -N -X POST http://localhost:3100/chat/api -H "Content-Type: application/json" -d '{"chatId":"<seed chat id>","message":"안녕하세요","visibility":"private"}' --cookie "<로그인 쿠키>"
```
Expected: streaming 텍스트 응답. (key 없으면 이 step 보류 — Task 7 E2E와 함께 검증)

- [ ] **Step 5: 커밋**

```bash
git add "app/(app)/chat/api/route.ts"
git commit -m "feat: M2 v1.6 - streaming chat route (guards->classify->prompt->Gateway)"
```

---

## Task 6: Chat UI (목록·새 chat·thread)

**Files:** Create `components/chat/AreaBadge.tsx`, `components/chat/ChatMessage.tsx`, `components/chat/ChatComposer.tsx`, `components/chat/ChatList.tsx`, `app/(app)/chat/page.tsx`, `app/(app)/chat/new/page.tsx`, `app/(app)/chat/[chatId]/page.tsx`, `app/(app)/chat/new/actions.ts`

`@ai-sdk/react` `useChat`로 thread streaming. 응답 JSON에 `escalation`이 오면(가드 hit) 입력 가드 분기 → v1.5 `EmergencyEscalation`/`MentalHealthEscalation` 컴포넌트 render. 모든 thread 상단에 v1.5 `BetaDisclaimer`.

- [ ] **Step 1: AreaBadge + ChatMessage**

```tsx
// components/chat/AreaBadge.tsx
const LABEL: Record<string, string> = {
  A: "일상", B: "검사·시술", C: "마음", D: "부부", E: "결정", mixed: "대화",
};
export function AreaBadge({ area }: { area: string | null }) {
  if (!area) return null;
  return (
    <span className="bg-muted rounded px-2 py-0.5 text-xs">
      {LABEL[area] ?? area}
    </span>
  );
}
```

```tsx
// components/chat/ChatMessage.tsx
export function ChatMessage({
  role,
  content,
}: {
  role: "user" | "assistant";
  content: string;
}) {
  const mine = role === "user";
  return (
    <div className={mine ? "text-right" : "text-left"}>
      <div
        className={`inline-block max-w-[80%] rounded-lg px-3 py-2 text-sm whitespace-pre-wrap ${
          mine ? "bg-primary text-primary-foreground" : "bg-muted"
        }`}
      >
        {content}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: ChatComposer + ChatList**

```tsx
// components/chat/ChatComposer.tsx
"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export function ChatComposer({
  onSend,
  pending,
}: {
  onSend: (text: string) => void;
  pending: boolean;
}) {
  const [text, setText] = useState("");
  return (
    <form
      className="flex gap-2"
      onSubmit={(e) => {
        e.preventDefault();
        if (!text.trim()) return;
        onSend(text);
        setText("");
      }}
    >
      <Input
        value={text}
        onChange={(e) => setText(e.target.value)}
        placeholder="무엇이든 편하게 적어주세요"
        disabled={pending}
      />
      <Button type="submit" disabled={pending || !text.trim()}>
        보내기
      </Button>
    </form>
  );
}
```

```tsx
// components/chat/ChatList.tsx
import Link from "next/link";
import { AreaBadge } from "./AreaBadge";

type Row = {
  id: string;
  title: string | null;
  visibility: string;
  area: string | null;
};
export function ChatList({ chats }: { chats: Row[] }) {
  if (chats.length === 0)
    return <p className="text-muted-foreground text-sm">아직 대화가 없어요.</p>;
  return (
    <ul className="space-y-2">
      {chats.map((c) => (
        <li key={c.id}>
          <Link
            href={`/chat/${c.id}`}
            className="hover:bg-muted flex items-center justify-between rounded border p-3"
          >
            <span className="text-sm">{c.title ?? "새 대화"}</span>
            <span className="flex items-center gap-2">
              <AreaBadge area={c.area} />
              <span className="text-muted-foreground text-xs">
                {c.visibility === "pair" ? "함께" : "나만"}
              </span>
            </span>
          </Link>
        </li>
      ))}
    </ul>
  );
}
```

- [ ] **Step 3: chat 목록 페이지 + 새 chat (server action)**

```tsx
// app/(app)/chat/page.tsx
import Link from "next/link";
import { listChats } from "@/lib/chat/queries";
import { ChatList } from "@/components/chat/ChatList";
import { Button } from "@/components/ui/button";

export default async function ChatListPage() {
  const chats = await listChats();
  return (
    <div className="space-y-4 py-8">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-semibold">AI 동반자</h1>
        <Button asChild>
          <Link href="/chat/new">새 대화</Link>
        </Button>
      </div>
      <ChatList chats={chats} />
    </div>
  );
}
```

```typescript
// app/(app)/chat/new/actions.ts
"use server";
import { createClient as createServiceClient } from "@supabase/supabase-js";
import { redirect } from "next/navigation";
import { requireUser } from "@/lib/auth/current-user";
import { getCoupleForUser } from "@/lib/couple/queries";
import { createChat } from "@/lib/chat/mutations";

const admin = () =>
  createServiceClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false } },
  );

export async function createChatAction(formData: FormData) {
  const user = await requireUser();
  const visibility =
    formData.get("visibility") === "private" ? "private" : "pair";
  const coupleId = await getCoupleForUser(user.id);
  if (!coupleId) return { error: "먼저 배우자와 연결해주세요." };
  const chat = await createChat(admin(), {
    coupleId,
    ownerUserId: user.id,
    visibility,
  });
  redirect(`/chat/${chat.id}`);
}
```

```tsx
// app/(app)/chat/new/page.tsx
import { createChatAction } from "./actions";
import { Button } from "@/components/ui/button";
import { Card, CardHeader, CardContent, CardFooter } from "@/components/ui/card";

export default function NewChatPage() {
  return (
    <Card className="mt-8 max-w-md">
      <CardHeader>
        <h1 className="text-xl font-semibold">새 대화 시작</h1>
        <p className="text-muted-foreground text-sm">
          이 대화를 배우자와 함께 볼지, 나만 볼지 정해주세요.
        </p>
      </CardHeader>
      <form action={createChatAction}>
        <CardContent className="space-y-3">
          <label className="flex items-center gap-2 text-sm">
            <input type="radio" name="visibility" value="pair" defaultChecked />
            함께 보기 (배우자와 공유)
          </label>
          <label className="flex items-center gap-2 text-sm">
            <input type="radio" name="visibility" value="private" />
            나만 보기
          </label>
        </CardContent>
        <CardFooter>
          <Button type="submit" className="w-full">
            시작하기
          </Button>
        </CardFooter>
      </form>
    </Card>
  );
}
```

- [ ] **Step 4: thread 페이지 (useChat + 가드 분기 + BETA 면책)**

```tsx
// app/(app)/chat/[chatId]/page.tsx
import { getChat, getMessages } from "@/lib/chat/queries";
import { ChatThread } from "./thread";

export default async function ChatPage({
  params,
}: {
  params: Promise<{ chatId: string }>;
}) {
  const { chatId } = await params;
  const chat = await getChat(chatId);
  if (!chat) return <p className="py-8">대화를 찾을 수 없어요.</p>;
  const messages = await getMessages(chatId);
  return (
    <ChatThread
      chatId={chatId}
      visibility={chat.visibility}
      initial={messages.map((m) => ({
        id: m.id,
        role: m.role as "user" | "assistant",
        content: m.content,
        guard: (m.guard_triggered as string[] | null) ?? null,
      }))}
    />
  );
}
```

```tsx
// app/(app)/chat/[chatId]/thread.tsx
"use client";
import { useState } from "react";
import { ChatMessage } from "@/components/chat/ChatMessage";
import { ChatComposer } from "@/components/chat/ChatComposer";
import { BetaDisclaimer } from "@/components/safety/BetaDisclaimer";
import { EmergencyEscalation } from "@/components/safety/EmergencyEscalation";
import { MentalHealthEscalation } from "@/components/safety/MentalHealthEscalation";

type Msg = { id: string; role: "user" | "assistant"; content: string; guard?: string[] | null };

export function ChatThread({
  chatId,
  visibility,
  initial,
}: {
  chatId: string;
  visibility: string;
  initial: Msg[];
}) {
  const [msgs, setMsgs] = useState<Msg[]>(initial);
  const [pending, setPending] = useState(false);
  const [escalation, setEscalation] = useState<string | null>(
    initial.find((m) => m.guard?.length)?.guard?.[0] ?? null,
  );

  async function send(text: string) {
    setPending(true);
    setMsgs((m) => [...m, { id: crypto.randomUUID(), role: "user", content: text }]);
    const res = await fetch("/chat/api", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chatId, message: text, visibility }),
    });
    const ct = res.headers.get("content-type") ?? "";
    if (ct.includes("application/json")) {
      const j = (await res.json()) as { escalation?: string };
      if (j.escalation) setEscalation(j.escalation);
      setPending(false);
      return;
    }
    const reader = res.body!.getReader();
    const dec = new TextDecoder();
    let acc = "";
    const id = crypto.randomUUID();
    setMsgs((m) => [...m, { id, role: "assistant", content: "" }]);
    for (;;) {
      const { done, value } = await reader.read();
      if (done) break;
      acc += dec.decode(value);
      setMsgs((m) =>
        m.map((x) => (x.id === id ? { ...x, content: acc } : x)),
      );
    }
    setPending(false);
  }

  return (
    <div className="space-y-4 py-6">
      <BetaDisclaimer />
      <div className="space-y-3">
        {msgs.map((m) => (
          <ChatMessage key={m.id} role={m.role} content={m.content} />
        ))}
      </div>
      {escalation === "mental_health_emergency" && <MentalHealthEscalation />}
      {escalation === "physical_emergency" && <EmergencyEscalation />}
      {!escalation && <ChatComposer onSend={send} pending={pending} />}
    </div>
  );
}
```

> v1.5 `BetaDisclaimer`/`EmergencyEscalation`/`MentalHealthEscalation`의 실제 props를 `components/safety/*` 에서 확인 후, 필요한 prop이 있으면 맞춰 전달한다(없으면 위와 같이 prop 없이 render).

- [ ] **Step 5: 빌드 + 손 검증(가드 경로는 key 불필요)**

Run: `npm run build`
Expected: 에러 0.
Run: `npm run dev` → 로그인 → `/chat/new` → 대화 생성 → thread에서 "심한 복통이 토하고 소변이 안 나와요" 입력
Expected: AI 호출 없이 `EmergencyEscalation` 박스 표시 (가드 경로는 key 불필요).

- [ ] **Step 6: 커밋**

```bash
git add components/chat/ "app/(app)/chat/"
git commit -m "feat: M2 v1.6 - chat UI (list/new/thread) + guard escalation branch"
```

---

## Task 7: E2E — chat 골든 path + 신체응급 분리

**Files:** Create `tests/e2e/ai-companion.spec.ts`

`couple-flow.spec.ts` 패턴. 신체응급(가드)은 key 불필요 — 항상 실행. 정상 streaming 응답은 `AI_GATEWAY_API_KEY` 있을 때만 (없으면 `test.skip`).

- [ ] **Step 1: 작성**

```typescript
// tests/e2e/ai-companion.spec.ts
import { test, expect, type Page } from "@playwright/test";

const TS = Date.now();
const PASS = "TestPass123!";
const HAS_KEY = !!process.env.AI_GATEWAY_API_KEY;

async function signup(page: Page, email: string) {
  await page.goto("/signup");
  await page.waitForLoadState("networkidle");
  await page.fill("input[name=email]", email);
  await page.fill("input[name=password]", PASS);
  await page.click("button[type=submit]");
  await page.waitForURL("**/dashboard", { timeout: 15_000 });
}

async function soloCouple(page: Page, email: string) {
  await signup(page, email);
  await page.click("text=배우자 초대하기");
  await page.waitForURL("**/couple/invite");
  await page.click("text=초대 링크 만들기");
  await page.locator('[data-testid="invite-url"]').waitFor();
}

test("physical emergency: escalation, no AI call (key not required)", async ({
  page,
}) => {
  await soloCouple(page, `ai-e-${TS}@example.com`);
  await page.goto("/chat/new");
  await page.click("text=시작하기");
  await page.waitForURL("**/chat/**");
  await page
    .locator("input")
    .fill("배가 심하게 부풀고 토하고 소변이 안 나와요");
  await page.click("text=보내기");
  await expect(page.getByText(/119|응급/)).toBeVisible();
});

test("chat golden path (requires AI_GATEWAY_API_KEY)", async ({ page }) => {
  test.skip(!HAS_KEY, "AI_GATEWAY_API_KEY 미설정 — 스트리밍 테스트 보류");
  await soloCouple(page, `ai-g-${TS}@example.com`);
  await page.goto("/chat/new");
  await page.click("text=시작하기");
  await page.waitForURL("**/chat/**");
  await page.locator("input").fill("커피 하루 한 잔은 괜찮을까요?");
  await page.click("text=보내기");
  await expect(page.locator(".bg-muted").last()).not.toBeEmpty({
    timeout: 20_000,
  });
});
```

- [ ] **Step 2: 실행**

Run: `npm run test:e2e -- ai-companion`
Expected: 신체응급 PASS. 골든 path는 key 있으면 PASS / 없으면 skip.

- [ ] **Step 3: 커밋**

```bash
git add tests/e2e/ai-companion.spec.ts
git commit -m "test: M2 v1.6 - chat E2E (emergency always, golden path key-gated)"
```

---

## Task 8: 검증 게이트 + 마무리

- [ ] **Step 1: 전체 단위 회귀**

Run: `npm run test:unit`
Expected: 기존 102 + 신규(area-router 6 + personal-context 2 + chat-rls 3) 모두 PASS.

- [ ] **Step 2: 빌드**

Run: `npm run build`
Expected: 에러 0.

- [ ] **Step 3: E2E**

Run: `npm run test:e2e`
Expected: 기존 3 + 신규(신체응급 항상, 골든 path key-gated) PASS/skip.

- [ ] **Step 4: 최종 커밋 (push·tag는 user 확인 후)**

```bash
git add -A
git commit -m "feat: M2 v1.6-chat complete - AI companion answers with guards (Gateway)"
```
> push 및 `v0.2.0-m2`-계열 태깅은 v1.6-rag·v1.6-diary까지 끝나고 M2 전체 완료 시 user 확인 후. (CLAUDE.md §12-6·8)

---

## Self-Review (작성자 점검 결과)

- **Spec 커버리지:** skeleton Task 3(AI SDK+Gateway=Task1·5)·6(area=Task2)·9(UI=Task6)·11(personal-context=Task3)·12 일부(chat E2E=Task7) 커버. Task 7(RAG)·8(retrieve)·10(diary)은 명시적 비범위(별도 plan).
- **전제 의존:** Task 5·7의 실제 streaming verify는 `AI_GATEWAY_API_KEY` 필요 — 해당 step에 "key 없으면 보류" 명시, 나머지(가드·UI·RLS·분류)는 key 없이 진행·검증 가능하게 분리.
- **v1.5 정합 리스크:** 가드 export 시그니처(`detectPhysicalEmergency`/`detectMentalHealthEmergency`)·safety 컴포넌트 props는 추정 — Task 5 Step 1, Task 6 Step 4에 "실제 파일 확인 후 정합" 명시. 구현자는 이 확인 step을 건너뛰지 말 것.
- **타입 일관성:** `PersonalContext`(system-prompt.ts)·`Persona`(persona-router.ts)·`createChat`/`addMessage`(mutations.ts) 시그니처가 Task 3·4·5에서 일관.
- **No placeholder:** 모든 코드 step에 실제 코드 포함. "확인 후 정합" step은 의도된 검증 단계(placeholder 아님).
